[project]
name = "subchef-ia"
version = "0.1.0"
description = "AI-powered web app that creates recipes from your available ingredients."
readme = "README.md"
requires-python = "==3.10.*"

[dependency-groups]
dev = [
    "ruff>=0.12.0",
    "setuptools>=80.9.0",
]
